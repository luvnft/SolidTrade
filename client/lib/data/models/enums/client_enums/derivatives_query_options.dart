enum DerivativesSortOptions {
  leverage,
  strike,
  knockout,
  delta,
  size,
  expiry,
}

enum DerivativesSortDirectionOptions {
  asc,
  desc,
}

enum DerivativesOptionType {
  long,
  short,
  call,
  put,
}
